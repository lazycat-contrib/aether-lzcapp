# Aether - LazyCat Build Configuration (LPK v2)

# Package metadata file (LPK v2 required)
package: ./package.yml

# Runtime manifest file
manifest: ./lzc-manifest.yml

# Output directory for lpk package
pkgout: ./

# Application icon (512x512 PNG required)
icon: ./icon.png
